import { config } from 'dotenv';
config();

// This file can be used as an entry point for development and testing.
// The primary AI flows for the application are now implemented using direct API calls
// and are imported directly by the components that use them.
